## 2024-05-23 - Navigation Accessibility
**Learning:** Wrapping link collections in `<nav>` and using `aria-current="page"` is a critical pattern for assistive technology users to understand page structure and location.
**Action:** Always use `<nav>` for primary navigation and explicitly mark the active page state with `aria-current`, not just visual styles.
